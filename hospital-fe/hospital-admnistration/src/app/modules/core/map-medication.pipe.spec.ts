import { MapMedicationPipe } from './map-medication.pipe';

describe('MapMedicationPipe', () => {
  it('create an instance', () => {
    const pipe = new MapMedicationPipe();
    expect(pipe).toBeTruthy();
  });
});
