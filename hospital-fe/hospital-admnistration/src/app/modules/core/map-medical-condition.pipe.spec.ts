import { MapMedicalConditionPipe } from './map-medical-condition.pipe';

describe('MapMedicalConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new MapMedicalConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
